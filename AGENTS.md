# Repository instructions

- `test_guns_2d_addon/**` and the root `Test_Guns_2D_Addon.mcaddon` are immutable third-party artifacts. Never edit, rebuild, repackage, or replace them.
- Put navigation, HUD, and cross-addon compatibility changes in the consuming add-on instead of Test Guns.
