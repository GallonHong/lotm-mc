{
  "format_version": "1.8.0",
  "animations": {
    "animation.tg_shield.wield_first_person": {
      "loop": true,
      "bones": {
        "shield": {
          "position": [
            "query.is_sneaking ? -0.8 : -6.0",
            "query.is_sneaking ? 4.5 : -5.5",
            "query.is_sneaking ? -4.5 : 1.0"
          ],
          "rotation": [
            "query.is_sneaking ? 0.0 : -22.0",
            "query.is_sneaking ? 0.0 : 38.0",
            "query.is_sneaking ? 0.0 : -16.0"
          ]
        }
      }
    },
    "animation.tg_shield.wield_third_person": {
      "loop": true,
      "bones": {
        "shield": {
          "position": [
            "query.is_sneaking ? 0.0 : 0.0",
            "query.is_sneaking ? 2.0 : 0.0",
            "query.is_sneaking ? -2.0 : 0.0"
          ],
          "rotation": [
            "query.is_sneaking ? -10.0 : 0.0",
            "query.is_sneaking ? 15.0 : 0.0",
            0.0
          ]
        }
      }
    }
  }
}