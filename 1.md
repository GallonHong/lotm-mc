# Minecraft Bedrock 末日枪械生存服务器 MVP 开发任务

## 0. 项目目标

在当前 Minecraft Bedrock Dedicated Server + Script API 项目中，开发第一版“末日生存枪械 MVP”。

本阶段不要开发完整《明日之后》系统，不要大规模重构现有服务器框架。

目标是先跑通以下核心循环：

```text
获得/制作枪械图纸
→ 收集制造材料
→ 消耗1张图纸制作1把枪
→ 装填弹药
→ 使用枪械无原版伤害间隔射击
→ 击杀感染者/测试目标
→ 武器消耗耐久
→ 再次获得图纸与材料制造
```

当前项目已经存在服务器基础框架，包括但不限于：

- 金币；
- 商店；
- 抽奖；
- 管理员系统；
- UI；
- 玩家数据；
- 领地等。

优先复用现有框架，不重新开发这些底层系统。

---

# 1. MVP 范围

第一版只实现 4 把枪：

```text
M1911
AKM / AK47
M870
MP5
```

其中：

- M1911：半自动手枪；
- AKM：全自动步枪；
- M870：泵动霰弹枪；
- MP5：高射速冲锋枪。

第一版只需要验证：

```text
半自动
全自动
霰弹
高射速自动武器
```

四种枪械框架。

不要一次性迁移 DeadZone 全部武器。

---

# 2. DeadZone 资源临时复用

仓库中存在：

```text
V1.6.6-1DeadZone/
├── DeadZzoneBp/
└── DeadZzoneRp/
```

MVP 阶段允许临时复用其中的枪械视觉资源，用于内部技术验证。

重点查找并复用：

```text
DeadZzoneRp/animation_controllers/guns/
DeadZzoneRp/animations/guns.animation/
DeadZzoneRp/models/
DeadZzoneRp/textures/
DeadZzoneRp/sounds/
```

优先选择：

```text
AK47
M1911
M870
MP5
```

相关资源。

允许临时复用：

- 枪械 geometry；
- 枪械贴图；
- 通用持枪姿势；
- draw 动画；
- hold 动画；
- sprint 动画；
- aim 动画；
- reload 动画；
- fire 动画；
- 第三人称持枪动作；
- 相关枪声。

## 重要

DeadZone 当前资源许可证不允许未经授权再分发。

因此：

```text
这些资源只能作为 MVP / 本地 / 内部测试临时资源。
```

代码中必须把 DeadZone 资源隔离到明确目录，例如：

```text
resource_pack/
└── temporary_deadzone_assets/
```

或者增加：

```text
TEMP_DEADZONE_ASSET
```

注释/标记。

禁止：

- 修改后宣称为自制；
- 将这些资源作为最终正式商业服资源；
- 将 DeadZone 内容混入不可区分的原创目录。

后续必须可以整体替换为自制 Blockbench 资源，而无需重写枪械逻辑。

因此：

> 枪械逻辑不得依赖某一个 DeadZone 特定模型结构。

建立一层视觉配置映射。

---

# 3. 枪械架构

建立统一：

```text
GunRegistry
GunController
FireScheduler
AmmoManager
ReloadManager
HitResolver
FirearmDamageResolver
GunAnimationBridge
GunDurabilityManager
BlueprintManager
WeaponCraftingManager
```

不要为每把枪复制一整套脚本。

---

# 4. GunDefinition

至少定义：

```ts
interface GunDefinition {
  id: string;

  category:
    | "pistol"
    | "assault_rifle"
    | "smg"
    | "shotgun";

  damage: number;

  rpm: number;

  magazineSize: number;

  reloadSeconds: number;

  range: number;

  spreadHip: number;

  spreadAds: number;

  durabilityMax: number;

  durabilityPerShot: number;

  ammoType: string;

  fireMode:
    | "semi"
    | "auto"
    | "pump";

  pellets?: number;

  animationProfile: string;

  soundProfile: string;
}
```

---

# 5. 第一版枪械数值

## M1911

```yaml
damage: 18
rpm: 300
magazine: 7
reload: 1.8
range: 32
fire_mode: semi
durability: 700
ammo: survival:ammo_45
```

定位：

```text
初始武器
高单发
低射速
```

---

## AKM

```yaml
damage: 13
rpm: 600
magazine: 30
reload: 2.6
range: 46
fire_mode: auto
durability: 1300
ammo: survival:ammo_762
```

定位：

```text
普通主力步枪
```

---

## MP5

```yaml
damage: 9
rpm: 900
magazine: 30
reload: 2.1
range: 30
fire_mode: auto
durability: 1100
ammo: survival:ammo_9mm
```

定位：

```text
近距离高射速
```

---

## M870

每次射击：

```yaml
pellets: 8
damage_per_pellet_pve: 6
damage_per_pellet_pvp: 3
rpm: 75
magazine: 6
range: 20
fire_mode: pump
durability: 900
ammo: survival:ammo_12g
```

增加明显距离衰减。

---

# 6. 禁止依赖 Minecraft 原版受伤无敌帧

这是最高优先级要求。

Minecraft 原生 `applyDamage()` 存在受伤无敌帧，会导致：

```text
600 RPM
900 RPM
```

等自动武器大量子弹命中但不实际造成伤害。

因此枪械不得简单实现为：

```ts
target.applyDamage(damage);
```

作为最终伤害方式。

建立：

```text
FirearmDamageResolver
```

枪械伤害必须独立于原版普通受击间隔。

推荐：

```text
射击
↓
服务端 Raycast
↓
HitResolver
↓
计算最终伤害
↓
读取 minecraft:health
↓
服务端扣除对应生命值
```

概念：

```ts
const health = target.getComponent("minecraft:health");

const nextHealth =
  Math.max(
    0,
    health.currentValue - finalDamage
  );

health.setCurrentValue(nextHealth);
```

注意：

这只是概念结构。

需要正确处理：

- 实体死亡；
- 击杀归属；
- Boss；
- 玩家；
- 护甲；
- 距离衰减；
- 枪械 Buff；
- 后续特殊技能。

不要修改：

```text
摔落
岩浆
僵尸攻击
近战
环境伤害
```

只建立：

```text
Firearm Damage Layer
```

---

# 7. FireScheduler

服务器为：

```text
20 TPS
```

不能假设所有 RPM 都能转换为整数 tick。

实现累加器：

```ts
accumulator += rpm / 60 / 20;

while (accumulator >= 1) {
  fireShot();
  accumulator -= 1;
}
```

验证：

```text
300 RPM = 5发/s
600 RPM = 10发/s
900 RPM = 15发/s
1200 RPM = 20发/s
```

第一版允许最高：

```text
1200 RPM
```

不要超过。

必须确保：

```text
MP5 900 RPM
```

实际10秒射击数量接近：

```text
150发
```

而不是被 tick 四舍五入后严重偏差。

---

# 8. 服务端权威射击

不要相信客户端：

```text
“我击中了谁”
```

服务器只接受：

```text
玩家正在尝试射击
```

由服务器检查：

1. 当前手持物是否为合法枪械；
2. 是否处于 reload；
3. 是否达到 RPM 间隔；
4. 弹匣是否有弹；
5. 是否处于禁止射击状态；
6. 当前枪口位置；
7. 玩家视线方向；
8. Spread；
9. Raycast；
10. 命中目标；
11. 最终伤害。

最终伤害完全由服务器计算。

---

# 9. Shot / Hit / Damage 分离

建立三个事件：

```text
SHOT
HIT
DAMAGE
```

## SHOT

负责：

- 弹匣 -1；
- 枪声；
- 枪口焰；
- 后坐力；
- 耐久；
- 射击动画。

## HIT

负责：

- Hitmarker；
- 血花；
- 命中音效；
- 命中统计；
- 后续限定武器技能充能。

## DAMAGE

负责：

- 基础伤害；
- 距离衰减；
- 护甲；
- PvE / PvP；
- 最终扣血。

三层不得混为一个函数。

---

# 10. 第一版命中部位

MVP 暂时不要做复杂骨骼 Hitbox。

只区分：

```text
head
body
```

如果当前 Bedrock Raycast 难以稳定区分头部：

第一版允许只实现：

```text
body
```

但架构必须预留：

```ts
hitZone:
  | "head"
  | "body"
  | "limb";
```

后续再加入爆头。

---

# 11. 弹药系统

建立：

```text
AmmoRegistry
```

MVP：

```text
survival:ammo_45
survival:ammo_9mm
survival:ammo_762
survival:ammo_12g
```

枪械弹匣数据必须由服务器维护。

至少保存：

```text
currentMagazineAmmo
```

不要只通过物品 Lore 解析。

---

# 12. Reload

需要：

```text
ReloadManager
```

流程：

```text
开始换弹
↓
进入 reload state
↓
播放 reload 动画
↓
禁止射击
↓
等待动画关键时间
↓
从玩家背包扣除弹药
↓
补充弹匣
↓
退出 reload
```

如果玩家：

```text
换枪
死亡
掉落武器
进入副本切换状态
```

换弹必须取消。

不得：

```text
开始换弹时立即扣弹
```

优先在换弹完成点结算。

---

# 13. DeadZone 动画适配

参考 DeadZone 已存在的状态：

```text
draw
holding
sprint
aim
back
reload
```

我们的 MVP 状态：

```text
equip
idle
fire
ads
sprint
reload
swim
```

临时允许直接绑定 DeadZone 对应动画。

但建立：

```ts
GunAnimationProfile {
  equip;
  idle;
  fire;
  ads;
  sprint;
  reload;
  swim;
}
```

枪械逻辑只调用：

```text
animationProfile.reload
```

不得在核心逻辑里写死：

```text
animation.ak47.xxx
```

这样后续替换原创动画时不需要改枪械逻辑。

---

# 14. 图纸系统核心规则

图纸为：

> **一次性制造材料**

不是永久解锁。

规则：

```text
1张图纸
+
1套制造材料

↓

1把对应武器
```

制造成功后：

```text
图纸 ×1 消耗
```

想制作第二把：

```text
必须再次获得图纸
```

---

# 15. 普通武器图纸允许玩家合成

MVP：

```text
M1911图纸
MP5图纸
M870图纸
AKM图纸
```

都允许玩家制作。

增加：

```text
图纸研究台
```

或者第一版直接通过现有 Form UI：

```text
武器制作
→ 图纸制作
```

---

# 16. 图纸制作材料

先使用简单资源占位。

## M1911 图纸

```text
基础枪械残页 ×8
机械研究数据 ×4
纸 ×2
```

## MP5 图纸

```text
冲锋枪残页 ×16
机械研究数据 ×10
纸 ×3
```

## M870 图纸

```text
霰弹枪残页 ×16
机械研究数据 ×10
纸 ×3
```

## AKM 图纸

```text
步枪残页 ×24
机械研究数据 ×18
枪械结构样本 ×1
纸 ×4
```

具体资源后续地图系统再替换。

---

# 17. 武器制造

图纸和实际武器材料必须分开。

## M1911

```text
M1911图纸 ×1
钢材 ×8
机械零件 ×4
枪管 ×1
```

## MP5

```text
MP5图纸 ×1
钢材 ×14
机械零件 ×8
聚合物 ×4
枪管 ×1
```

## M870

```text
M870图纸 ×1
钢材 ×16
机械零件 ×7
木材/聚合物 ×6
枪管 ×1
```

## AKM

```text
AKM图纸 ×1
钢材 ×18
机械零件 ×12
聚合物 ×5
枪管 ×1
```

成功：

```text
消耗全部材料
+
图纸
↓
获得1把枪
```

失败：

```text
不得扣除任何材料
```

需要事务式校验。

---

# 18. 高级 / 限定图纸架构预留

MVP 暂时不做限定武器内容，但 BlueprintDefinition 必须支持：

```ts
interface BlueprintDefinition {
  id: string;

  weaponId: string;

  playerCraftable: boolean;

  consumedOnCraft: true;

  rarity:
    | "basic"
    | "normal"
    | "advanced"
    | "limited";

  globalSupply?: number;

  serialEnabled?: boolean;
}
```

普通：

```text
playerCraftable = true
```

高级：

```text
playerCraftable = false
```

限定：

```text
playerCraftable = false
globalSupply = N
serialEnabled = true
```

以后抽奖终极大奖：

```text
只发放限定图纸
```

而不是直接发枪。

---

# 19. 武器耐久

枪械必须拥有独立耐久。

射击：

```text
每发消耗耐久
```

普通枪最终允许报废。

MVP 暂定：

```text
M1911 700发
MP5 1100发
M870 900次射击
AKM 1300发
```

耐久归零：

```text
无法射击
```

第一版可以先不做维修 UI，但必须保留数据。

---

# 20. HUD

使用现有服务器 UI / Actionbar 框架。

持枪时显示：

```text
AKM | 27 / 146
```

换弹：

```text
AKM | Reloading...
```

耐久低于20%：

```text
⚠ 武器耐久较低
```

不要每 tick 创建复杂 Form。

---

# 21. 测试感染者

如果项目已有感染者可直接使用。

没有则创建一个：

```text
survival:test_infected
```

参数：

```text
HP: 500
移动速度：低
攻击：可关闭
```

另外创建：

```text
survival:damage_dummy
```

HP：

```text
5000
```

用于 DPS 测试。

---

# 22. 必须做的自动测试

## 射速测试

连续射击10秒：

```text
M1911：
理论约50发

AKM：
理论约100发

MP5：
理论约150发
```

实际误差：

```text
≤ 2%
```

---

## 伤害间隔测试

目标：

```text
5000 HP
```

AKM连续命中10发：

预期：

```text
13 × 10 = 130
```

最终生命：

```text
4870
```

不得因为原版无敌帧只掉：

```text
26
39
```

之类异常伤害。

MP5同样验证900 RPM持续命中。

---

# 23. 性能要求

枪械不得：

- 每发生成长期存在 projectile 实体；
- 每发创建大量滞留粒子；
- 每把枪单独创建永久高频 interval；
- 每 tick 扫描全世界实体。

建议：

```text
统一 GunScheduler
```

负责所有在线玩家枪械。

射击优先使用：

```text
Raycast / 服务端命中计算
```

视觉弹道单独处理。

---

# 24. 文件结构建议

```text
scripts/
└── guns/
    ├── GunRegistry.ts
    ├── GunController.ts
    ├── FireScheduler.ts
    ├── HitResolver.ts
    ├── FirearmDamageResolver.ts
    ├── AmmoManager.ts
    ├── ReloadManager.ts
    ├── GunDurabilityManager.ts
    ├── GunAnimationBridge.ts
    ├── BlueprintManager.ts
    ├── WeaponCraftingManager.ts
    └── definitions/
        ├── m1911.ts
        ├── akm.ts
        ├── mp5.ts
        └── m870.ts
```

资源：

```text
RP/
├── temporary_deadzone_assets/
└── gun_bindings/
```

后续原创资源直接替换：

```text
temporary_deadzone_assets
```

而不修改枪械核心。

---

# 25. 不要做的事情

本 MVP 禁止扩展到：

- 庄园完整系统；
- 大型地图；
- 营地；
- 公会；
- 载具；
- 50把枪；
- 配件系统；
- 枪械品质随机；
- 限定武器；
- 枪械技能；
- 复杂爆头 Hitbox；
- 完整反作弊；
- 完整 Web 联动；
- 复杂 NPC；
- 大量感染者类型。

当前只验证：

> **枪能不能打得爽、伤害间隔能不能绕开、动画资源能不能正常复用、弹匣/换弹是否稳定、图纸一次性制造循环是否成立。**

---

# 26. 开发过程要求

每完成一个阶段立即测试，不要一次改完整项目。

顺序：

```text
STEP 1
只做 M1911

→ 单发
→ 弹匣
→ 换弹
→ DamageResolver

STEP 2
AKM

→ 600 RPM自动射击
→ 连续伤害

STEP 3
MP5

→ 900 RPM验证 FireScheduler

STEP 4
M870

→ 多弹丸

STEP 5
接入 DeadZone 动画资源

STEP 6
图纸制作

STEP 7
武器制作

STEP 8
耐久

STEP 9
整体验收
```

如果同一问题尝试修复两次仍失败，或者遇到：

- Script API 限制；
- 原版无敌帧；
- 高射速结算；
- DeadZone 动画绑定；
- 玩家动画控制器冲突；
- `minecraft:health` 自定义伤害死亡异常；
- 资源包兼容问题；

停止继续猜测，整理：

```text
问题
错误日志
相关文件
已尝试方案
当前理解
```

交给 Mac Codex 进行审查。

---

# 27. MVP 完成标准

只有全部达到才算完成：

- [ ] M1911 可以射击；
- [ ] AKM 600 RPM 实际正常；
- [ ] MP5 900 RPM 实际正常；
- [ ] M870 多弹丸正常；
- [ ] 自动武器不受原版伤害无敌帧影响；
- [ ] 弹匣正常；
- [ ] 换弹正常；
- [ ] DeadZone 临时枪模/动作可以正常显示；
- [ ] Draw / Hold / ADS / Sprint / Reload 至少可用；
- [ ] 图纸为一次性物品；
- [ ] 1张图纸只能制造1把枪；
- [ ] 普通图纸可由玩家合成；
- [ ] 制造失败不吞材料；
- [ ] 枪械有耐久；
- [ ] 所有枪械使用统一 Registry，而非重复硬编码；
- [ ] DeadZone 资源和原创代码严格隔离，确保后续可完全替换资源。

最终提交：

```text
1. 实现代码
2. 修改文件清单
3. 架构说明
4. 已知问题
5. 测试结果
6. DeadZone 临时资源依赖列表
7. 后续替换原创资源需要修改的位置
```