{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.apex.trigger_probe": {
      "initial_state": "released",
      "states": {
        "released": {
          "transitions": [
            {
              "pressed": "q.is_alive && q.main_hand_item_use_duration > 0.0 && q.is_item_name_any('slot.weapon.mainhand', 0, 'apex:ak47')"
            }
          ]
        },
        "pressed": {
          "on_entry": [
            "/execute as @s run scriptevent apex:trigger_down"
          ],
          "on_exit": [
            "/execute as @s run scriptevent apex:trigger_up"
          ],
          "transitions": [
            {
              "released": "!q.is_alive || q.main_hand_item_use_duration <= 0.0 || !q.is_item_name_any('slot.weapon.mainhand', 0, 'apex:ak47')"
            }
          ]
        }
      }
    }
  }
}
